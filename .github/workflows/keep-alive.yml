name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hit Render (home)
        run: curl -fsS --retry 2 --max-time 10 "${{ secrets.KEEPALIVE_URL }}" -o /dev/null
